[pytest]
describe_prefixes = describe context
# filterwarnings = ignore::DeprecationWarning 
# matplotlib throws a useless deprecation warning
required_plugins = "pytest-describe" "pytest-pspec" "pytest-raises"
# in a development environment, strongly recommend pytest-watch or pytest-watcher as well, but neither is required to run pytest